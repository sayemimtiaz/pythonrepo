from sklearn.model_selection import KFold
from sklearn.model_selection import cross_val_score
from ..foo.bar import a as b, c
import a
from .a import c
from ... import nib
import numpy as np
from a import b

a=2 if 3>2 else 4

print(2 if 3>2 else 4)

p(2 if 3>2 else 4)

a=((letter,p) for letter,p in 'human')

f(dict((l,i) for l,i in enumerate("mmm")))


print(dict((l,i) for l,i in enumerate("mmm")))

reverse_input_char_index = dict(
    (i, char) for char, i in input_token_index.items())

